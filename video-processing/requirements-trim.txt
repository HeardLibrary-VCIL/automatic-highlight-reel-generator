# Minimal deps for the head/tail trimming backend (main.py -> analyze_deadspace).
# ffmpeg is provided by the OS layer (see Dockerfile), not pip.
boto3==1.39.12
botocore==1.39.12
numpy==2.1.2
opencv-python-headless==4.12.0.88
